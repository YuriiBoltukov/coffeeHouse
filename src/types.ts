export interface SizeData {
  size: string;
  volume: string;
  text: string;
}

export interface AdditiveData {
  additive: string;
  name: string;
  text: string;
  label: string;
}

export type Category = 'coffee' | 'tea' | 'dessert';


